using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class VolumeSlider : MonoBehaviour
{
	//[SerializeField] private Slider m_VolumeSlider;

	//private void Start()
	//{
	//	SoundManager.Instance.ChangeMasterVolume(m_VolumeSlider.value);
	//	m_VolumeSlider.onValueChanged.AddListener(val => SoundManager.Instance.ChangeMasterVolume(val));
	//}
}
